<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tiny11 Builder"
        Height="650"
        Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3"
        FontFamily="Segoe UI Variable Display, Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1A1A1A"/>
        </Style>
        
        <Style x:Key="HeaderBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.05" ShadowDepth="2" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0067C0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E5E5E5"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Tiny11 Image Builder" Style="{StaticResource HeaderBlock}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Source Section -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="1. Source Configuration" FontWeight="Bold" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Windows ISO File or Mounted Drive" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Name="IsoBox"/>
                            <Button Name="BrowseBtn" Grid.Column="1" Content="Browse" Width="80" Margin="5,0,0,0" Style="{StaticResource SecondaryButton}"/>
                            <Button Name="ScanBtn" Grid.Column="2" Content="Scan" Width="80" Margin="5,0,0,0" Style="{StaticResource ModernButton}"/>
                        </Grid>

                        <TextBlock Text="Windows Edition" Margin="0,0,0,5"/>
                        <ComboBox Name="IndexCombo" DisplayMemberPath="ImageName" SelectedValuePath="ImageIndex" Margin="0,0,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Options Section -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="2. Build Options" FontWeight="Bold" Margin="0,0,0,15"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Scratch Drive (Temporary Space)"/>
                            <Viewbox Width="14" Height="14" Margin="5,0,0,0" ToolTipService.InitialShowDelay="100">
                                <Grid Background="Transparent" ToolTip="Temporary workspace for extracting and modifying Windows files. Requires 15-20GB of free space (SSD recommended).">
                                    <Path Data="M12,2A10,10 0 1,0 22,12A10,10 0 0,0 12,2M11,17V11H13V17H11M11,9V7H13V9H11Z" Fill="#0067C0"/>
                                </Grid>
                            </Viewbox>
                        </StackPanel>
                        <TextBox Name="ScratchBox" Text="C" Margin="0,0,0,15"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <CheckBox Name="EdgeCheck" Content="Remove Microsoft Edge" IsChecked="True" Margin="0,0,20,0"/>
                            <CheckBox Name="TelemetryCheck" Content="Disable Telemetry" IsChecked="True"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Status Section -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Name="StatusText" Text="Ready to build" Margin="0,0,0,10" Foreground="#666666"/>
                        <ProgressBar Name="ProgressBar" Height="6" BorderThickness="0" Background="#E5E5E5" Foreground="#0067C0" Margin="0,0,0,15"/>
                        
                        <Button Name="BuildBtn" Content="Start Building Tiny11" Height="45" Style="{StaticResource ModernButton}" FontSize="16"/>
                        
                        <TextBox Name="LogBox"
                                 Margin="0,15,0,0"
                                 AcceptsReturn="True"
                                 IsReadOnly="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Height="150"
                                 Background="#FAFAFA"
                                 BorderBrush="#E5E5E5"
                                 FontFamily="Consolas"
                                 FontSize="12"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
